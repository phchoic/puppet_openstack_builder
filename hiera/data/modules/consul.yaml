consul::config_defaults:
  data_dir: '/var/lib/consul'
  bind_addr: "%{hiera('mgmt_address')}"
  enable_syslog: true
  log_level: INFO
  node_name: "%{::hostname}"
  retry_join:
    - "%{hiera('build_mgmt')}"

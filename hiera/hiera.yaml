:backends:
  - yaml
:yaml:
   :datadir: /etc/puppet/hiera/data
:hierarchy:
  - "hostname/%{hostname}"
  - "user.%{role}"
  - user
  - "roles/%{role}"
  - "classes/%{calling_class}"
  - "modules/%{calling_module}"
  - roles
  - common

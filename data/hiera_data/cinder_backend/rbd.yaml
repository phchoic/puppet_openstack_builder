#################
# Data Mappings #
#################

# cinder_rbd_pool
cephdeploy::osdwrapper::cinder_rbd_pool: "%{hiera('cinder_rbd_pool')}"
cinder::volume::rbd::rbd_pool: "%{hiera('cinder_rbd_pool')}"

# ceph_monitor_secret
cephdeploy::ceph_monitor_secret: "%{hiera('ceph_monitor_secret')}"

# ceph_deploy_user
cephdeploy::ceph_deploy_user: "%{hiera('ceph_deploy_user')}"
cephdeploy::client::ceph_deploy_user: "%{hiera('ceph_deploy_user')}"
cephdeploy::mon::ceph_deploy_user: "%{hiera('ceph_deploy_user')}"
cephdeploy::osdwrapper::ceph_deploy_user: "%{hiera('ceph_deploy_user')}"

# ceph_deploy_password
cephdeploy::ceph_deploy_password: "%{hiera('ceph_deploy_password')}"
cephdeploy::client::ceph_deploy_password: "%{hiera('ceph_deploy_password')}"

# glance_ceph_pool
cephdeploy::osdwrapper::glance_ceph_pool: "%{hiera('glance_ceph_pool')}"

# mon_initial_members
cephdeploy::mon_initial_members: "%{hiera('mon_initial_members')}"

# ceph_cluster_network
cephdeploy::ceph_cluster_network: "%{hiera('ceph_cluster_network')}"
cephdeploy::osdwrapper::ceph_cluster_network: "%{hiera('ceph_cluster_network')}"

# ceph_monitor_address
cephdeploy::ceph_monitor_address: "%{hiera('ceph_monitor_address')}"

# ceph_cluster_interface
cephdeploy::osdwrapper::ceph_cluster_interface: "%{hiera('ceph_cluster_interface')}"

# ceph_public_interface
cephdeploy::mon::ceph_public_interface: "%{hiera('ceph_public_interface')}"

# ceph_cluster_name
cephdeploy::mon::ceph_cluster_name: "%{hiera('ceph_cluster_name')}"

# ceph_primary_mon
cephdeploy::client::primary_mon: "%{hiera('ceph_primary_mon')}"
cephdeploy::mon::ceph_primary_mon: "%{hiera('ceph_primary_mon')}"
cephdeploy::osdwrapper::ceph_primary_mon: "%{hiera('ceph_primary_mon')}"

# ceph_monitor_fsid
cephdeploy::ceph_monitor_fsid: "%{hiera('ceph_monitor_fsid')}"
cinder::volume::rbd::rbd_secret_uuid: "%{hiera('ceph_monitor_fsid')}"

# ceph_openstack_user
cinder::volume::rbd::rbd_user: "%{hiera('ceph_openstack_user')}"

# ceph_public_network
cephdeploy::ceph_public_network: "%{hiera('ceph_public_network')}"
cephdeploy::mon::ceph_public_network: "%{hiera('ceph_public_network')}"

# ceph_configuration_file
cinder::volumes::rbd::rbd_ceph_conf: "%{hiera('ceph_configuration_file')}"
